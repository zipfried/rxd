# rxd - Rust X Downloader

## Usage

```
$ rxd download --help
Download with a config file

Usage: rxd.exe download <CONFIG_PATH>

Arguments:
  <CONFIG_PATH>  Path to config file

Options:
  -h, --help  Print help
```
